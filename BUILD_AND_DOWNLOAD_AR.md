# SILENT RAID — بناء APK ورابط تحميل مباشر

هذا المشروع جاهز للبناء على GitHub Actions بدون Android Studio على جهازك.

## الخطوات

1. أنشئ مستودع GitHub جديدًا، ويفضل أن يكون **Public** إذا أردت رابط تنزيل عام بدون تسجيل دخول.
2. ارفع محتويات هذا المجلد إلى المستودع، بحيث يظهر `build.gradle` و`settings.gradle` في الجذر.
3. افتح تبويب **Actions** ثم شغّل workflow باسم **Build and publish Silent Raid APK**.
4. اكتب tag مثل `v1.0.0` واضغط **Run workflow**.
5. بعد نجاح البناء افتح **Releases**. ستجد ملف APK مرفقًا بالـRelease، ومعه ملف SHA-256 للتحقق من سلامة الملف.

رابط الملف بعد النشر سيكون على نمط:

`https://github.com/OWNER/REPO/releases/download/v1.0.0/silent-raid-v1.0.0.apk`

استبدل `OWNER/REPO` باسم حسابك ومستودعك.

## الأمان

- التطبيق لا يحتاج إذن الإنترنت للوضع الحالي، وتمت إزالة خط Google Fonts الخارجي حتى تعمل اللعبة محليًا قدر الإمكان.
- APK الناتج هنا هو **debug-signed** لسهولة التثبيت والتجربة. هذا ليس بديلًا عن مفتاح توقيع Release تملكه أنت عند توزيع نسخة نهائية أو على متجر.
- ملف SHA-256 المرفق يسمح بمقارنة بصمة الملف بعد التنزيل.
- لا تعتمد على Artifact كحل للرابط الدائم؛ GitHub يضع له مدة احتفاظ، بينما Release هو الأنسب كرابط تنزيل عام طويل الأجل.
